# Lokalny stack Supabase (supabase start)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_KEY=your-local-anon-key

# service_role key — SERVER ONLY (Nitro routes). Never expose to client.
# Wartość: `supabase status` → SERVICE_ROLE_KEY
SUPABASE_SERVICE_KEY=your-local-service-role-key
